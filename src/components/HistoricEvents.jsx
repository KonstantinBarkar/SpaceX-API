import React from 'react';

const HistoricEvents = () => {
  return (
    <div>
      {/* 6. Using API History call, show a list of all historic events to the home page 
      7. Each item of the list should have "title", "details", "event_date_utc" in readable format and a link to the flight (flight_number prop) */}

      {/* git remote "test-2" */}
      

    </div>
  )
};

export default HistoricEvents;